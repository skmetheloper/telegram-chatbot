const { TELEGRAM_TOKEN } = process.env;
const { default: axios } = require('axios');

// const verify = (idToken) => null;

const handle = (req, res, next) => next():

module.exports = {
  verify,
  handle,
};
